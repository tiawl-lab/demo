---

- name: Check if the password-store repository was created
  ansible.builtin.include_tasks: "{{ playbook_dir }}/tasks/git-hoster/repository/created.yml"
  vars:
    repo_name: "{{ repo_passwordstore | split('/') | last }}"
    request_bearer: "{{ secrets_token }}"
