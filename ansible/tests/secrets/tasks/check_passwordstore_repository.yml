---

- name: Check if the password-store repository was created
  ansible.builtin.command: "{{ githoster_repo_list_cmd + ' ' + repo_owner + ' --json name --jq \"if (any(.name == \\\"secrets\\\") | not) then (halt_error(1)) end\"' }}"
  environment:
    GH_TOKEN: "{{ secrets_token }}"
