---

- name: Check the GnuPG key locally
  ansible.builtin.include_tasks: tests/secrets/tasks/check_local_gpgkey_present.yml
  vars:
    secrets_tests_gpgfingerprint: "{{ secrets_setup_gpg_fingerprint_importedkey }}"
